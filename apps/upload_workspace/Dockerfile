# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @watchme/upload_workspace`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t watchme-upload_workspace`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system watchme-upload_workspace && \
          adduser --system -G watchme-upload_workspace watchme-upload_workspace

COPY apps/upload_workspace/dist watchme-upload_workspace/
COPY apps/upload_workspace/package.json watchme-upload_workspace/
RUN chown -R watchme-upload_workspace:watchme-upload_workspace .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix watchme-upload_workspace --omit=dev -f install

CMD [ "node", "watchme-upload_workspace" ]
